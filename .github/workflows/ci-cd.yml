# Ce workflow GitHub Actions a été remplacé par Jenkins
# Voir Jenkinsfile et docker-compose.jenkins.yml pour la nouvelle configuration CI/CD

# Pour démarrer Jenkins:
# 1. Exécutez: docker-compose up -d jenkins mysql keycloak
# 2. Récupérez le mot de passe: docker exec tricol-jenkins cat /var/jenkins_home/secrets/initialAdminPassword
# 3. Accédez à http://localhost:8081
# 4. Configurez Jenkins avec les plugins requis
# 5. Créez un pipeline pointant vers ce repository